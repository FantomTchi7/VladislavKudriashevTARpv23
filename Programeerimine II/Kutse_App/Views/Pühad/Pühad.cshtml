@model IEnumerable<Kutse_App.Models.Pühad>

@{
    ViewBag.Title = "Pühad";
}

<h2>Pühad</h2>

<p>
    @Html.ActionLink("Loo uus", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Puhkuse_nimi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Kuupaev)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Puhkuse_nimi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Kuupaev)
        </td>
        <td>
            @Html.ActionLink("Muuda", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Kustuta", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
